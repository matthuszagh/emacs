;;; c-helm-notmuch.el ---  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(if (featurep 'straight)
    (straight-use-package 'helm-notmuch))

(require 'helm-notmuch)

(provide 'c-helm-notmuch)
;;; c-helm-notmuch.el ends here
