;;; c-time.el ---  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(require 'time)

;; enable display-time-mode by default
(setq display-time-mode t)
;; use 24h time
(setq display-time-24hr-format t)

(provide 'c-time)
;;; c-time.el ends here
