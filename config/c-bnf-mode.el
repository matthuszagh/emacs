;;; c-bnf-mode.el ---  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(if (featurep 'straight)
    (straight-use-package 'bnf-mode))

(require 'bnf-mode)

(provide 'c-bnf-mode)
;;; c-bnf-mode.el ends here
