;;; c-use-package.el ---  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(if (featurep 'straight)
    (straight-use-package 'use-package))

(require 'use-package)

(provide 'c-use-package)
;;; c-use-package.el ends here
