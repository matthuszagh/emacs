;;; c-simple.el ---  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(require 'simple)

;; enable column display in mode line
(column-number-mode)
;; display buffer size in mode line
(size-indication-mode)

(provide 'c-simple)
;;; c-simple.el ends here
