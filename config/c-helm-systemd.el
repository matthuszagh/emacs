;;; c-helm-systemd.el ---  -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(if (featurep 'straight)
    (straight-use-package 'helm-systemd))

(require 'helm-systemd)

(provide 'c-helm-systemd)
;;; c-helm-systemd.el ends here
