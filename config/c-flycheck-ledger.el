;;; c-flycheck-ledger.el --- flycheck-ledger configuration -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(if (featurep 'straight)
    (straight-use-package 'flycheck-ledger))

(require 'flycheck-ledger)

(provide 'c-flycheck-ledger)

;;; c-flycheck-ledger.el ends here
